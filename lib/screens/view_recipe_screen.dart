import 'package:flutter/material.dart';
import 'package:flutter_recipedia/widgets/snapping_app_bar.dart';

class ViewRecipeScreen extends StatelessWidget {
  const ViewRecipeScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SnappingAppBar();
  }
}
